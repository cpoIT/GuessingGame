function generateWinningNumber() {
  return Math.ceil(100*Math.random());
}

function shuffle (arr) {
let unshuffledArr = arr.slice();
for (let i = 0; i < arr.length; i++) {
  let a
}
return retArr;
}